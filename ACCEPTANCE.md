# 验收记录与人工测试

## 1. 自动化验收

以下项目已在本机执行：

| 项目 | 结果 | 证据 |
|---|---|---|
| Python Bridge / 协议语法 | 通过 | `python -m py_compile` |
| 浏览器 JavaScript 语法 | 通过 | `node --check web/app.js` |
| 协议往返测试 | 4/4 通过 | `test_protocol.py` |
| HTTPS 页面 | 通过 | `http://127.0.0.1:8080/` 返回 200 |
| HTTPS 健康接口 | 通过 | `/health` 返回 JSON |
| WSS 二进制发送 | 通过 | Node 测试发送 30 帧 |
| Bridge 转 UDP | 通过 | `pose_count` 从 0 增至 1 后又接收 30 帧 |
| Blender 插件注册 | 通过 | 当前 Blender 4.3.2 已注册 |
| Blender 演示模型生成 | 通过 | 42 关节点、40 连接骨、2 掌面 |
| Blender UDP 接收 | 通过 | 收到 117 帧，约 32 Hz 测试发送速率 |
| Blender 模型变化 | 通过 | 右手腕关节移动到约 `(0.226, -0.006, 0.025)` |
| 服务器到 Blender 闭环 | 通过 | 再接收 30 帧，累计从 117 到 147 |
| 对象模式驱动 | 通过 | 测试立方体 X 从 1.200 移动到 1.126 |
| 固定摄像机创建 | 通过 | `PHC_FixedCamera` 已设为活动相机并锁定视图 |
| 相机视锥坐标映射 | 通过 | 1280×720 等效画幅为 4.68×2.63，手机坐标按此换算 |
| 固定场景文件 | 通过 | 已生成 `hand_control_fixed_scene.blend` 与预览图 |

本机回环 RTT 测试为 1.53 ms。该数值不等于 Wi-Fi RTT，手机端最终数值应查看页面“网络 RTT”。

## 2. 手机现场验收步骤

把手机和 PC 连接到同一 Wi-Fi。路由器开启 AP 隔离时不可用。

1. 启动 Blender 插件接收器。
2. 运行 `.\run.ps1`。
3. 手机扫描 `join_qr.png`，完成 CA 安装。
4. 打开带 token 的 HTTPS 页面，授权摄像头。
5. 页面应显示安全上下文、WSS 已连接、手部数量和实时指标。
6. 移动手掌并观察 Blender 模型。
7. 打开/握拳并观察 21 个关节点。
8. 在 Blender 点击“中立位”。
9. 以不同速度和幅度移动 10 分钟。

## 3. 量化标准

| 指标 | 合格线 | 优秀 |
|---|---:|---:|
| 手机推理耗时 EMA | ≤ 50 ms | ≤ 30 ms |
| 追踪/发送帧率 | ≥ 24 Hz | 50-60 Hz |
| 手机网络 RTT | ≤ 35 ms | ≤ 15 ms |
| Blender 帧年龄 | ≤ 80 ms | ≤ 35 ms |
| 可见端到端延迟 | ≤ 160 ms | ≤ 100 ms |
| 静止抖动 | 肉眼轻微 | 几乎不可见 |
| 连续运行 | 5 分钟 | 30 分钟无断连 |

若推理耗时超过 50 ms：

- 确认使用 GPU 计算设备；
- 关闭其他高占用 App；
- 将分辨率降到 1280×720 或 960×540；
- 将发送上限设为 30 FPS；
- 不要通过 USB 或云端中转，当前局域网方案已经更短。

## 4. 精度验收动作

依次执行：

1. 手掌在画面中心保持不动 5 秒，再快速向右移。
2. 手掌沿垂直方向上下移动。
3. 手掌靠近和远离镜头。
4. 手指从完全张开到握拳，动作分 3 秒完成。
5. 手掌绕视线方向旋转 ±60°。
6. 左右手分别单独进入画面，检查手别标签。
7. 两只手同时进入画面，检查左右模型互不串扰。

验收要求：

- 左右移动方向符合自然镜像方向；若相反，打开/关闭“镜像 X”。
- 上下移动无反向。
- 握拳过程连续，无手指跨接或关节翻转。
- 手离镜头时不应出现大跳；如有可提高 One Euro 的静止稳定强度。
- 手快速运动时可以有极短拖尾，但不能持续滞后。
- 每只手的 21 点更新对应到正确的 Blender 关节。

## 5. 故障判定

- `isSecureContext=false`：证书问题，不是算法问题。
- 页面帧率正常但 Blender 不动：UDP 端口或插件接收器问题。
- Blender 帧年龄持续增长：Bridge 到 UDP 的链路异常，或 Blender 主线程被阻塞。
- 手机 RTT 正常但推理耗时超过 50 ms：GPU delegate 未启用或手机过热。
- 只有单手正常：检查手机画面中两只手是否互相遮挡，以及页面是否显示 2 只手。
- 手指准确但整体位置跳动：提高 One Euro 的静止稳定强度，减少预测补偿。


## 体积碰撞与交互自动化

- 手部使用 21 个关节球体、20 个指节胶囊和掌心球体作为碰撞体积。
- 左右手不建立刚体，也不参与互相碰撞。
- 交互物体使用球体或盒体碰撞体积，接触深度和手部速度共同产生冲量。
- `TOGGLE`、`BOUNCE`、`SPIN` 均通过实际碰撞触发。
- 摇杆弹球底座固定，球体通过弹性杆约束摆动，测试后回正误差约 0.002 m。
- 控制器使用安全定时器包装，单帧异常不会导致整个手部控制停止。
- Blender 后台验证脚本：`tests/blender_interactions_test.py`、`tests/blender_depth_test.py`。
- 前后空间测试：手掌图像放大时模型向相机移动，缩小时模型向场景深处移动。
